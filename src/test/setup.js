// setup.js -- Vitest setup file (see vite.config.js's test.setupFiles).
// Adds jest-dom's DOM matchers (toBeInTheDocument, etc.) for component
// tests written with @testing-library/react.
import "@testing-library/jest-dom/vitest";
